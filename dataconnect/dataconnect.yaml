specVersion: "v1beta"
serviceId: "project2"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "project2-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
